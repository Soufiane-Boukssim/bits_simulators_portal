import { SubFieldNamePipe } from './sub-field-name.pipe';

describe('SubFieldNamePipe', () => {
  it('create an instance', () => {
    // const pipe = new SubFieldNamePipe();
    // expect(pipe).toBeTruthy();
  });
});
