import { IpAddressMaskDirective } from './ip-address-mask.directive';

describe('IpAddressMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new IpAddressMaskDirective();
    expect(directive).toBeTruthy();
  });
});
