package com.simulator.atm.repositories.repoatm;

import com.simulator.entities.NDCStateType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NDCStateTypeRepository extends JpaRepository<NDCStateType, String> {
}
